<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bodyfatcontrol.github">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name="bodyfatcontrol.github.AnalyticsApplication"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="bodyfatcontrol.github.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="sensorPortrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="bodyfatcontrol.github.CreateFoodActivity" />
        <activity android:name="bodyfatcontrol.github.LogFoodMainActivity" />
        <activity android:name="bodyfatcontrol.github.LogFoodActivity" />
        <activity android:name="bodyfatcontrol.github.EditFoodActivity" />
        <activity android:name="bodyfatcontrol.github.EditLoggedFoodActivity" />
        <activity android:name="bodyfatcontrol.github.LogCaloriesFoodActivity" />
        <activity android:name="bodyfatcontrol.github.AboutActivity" />

        <service android:name=".ListenerService">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:path="/message_path" />
            </intent-filter>
        </service>
    </application>

</manifest>